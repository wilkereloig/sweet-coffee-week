# Achados — Participar

11 achados: 2 alta, 7 média, 2 baixa

> **Nota sobre linhas do CSS compartilhado.** O brief citava `scw-participar-apoiar.css:111, :556, :582, :666, :693`
> como as 5 ocorrências de `font: 600 ...` já confirmadas por `tests/regua-visual.mjs`. Reli o arquivo inteiro
> (818 linhas) e nenhuma dessas 5 linhas contém hoje uma regra de peso — são `margin-top`, `display: flex`,
> `grid-template-columns` e `padding-top` soltos, sem relação com tipografia. O arquivo claramente mudou desde a
> rodada que gerou aqueles números. Confirmei por `grep` que existem exatamente 5 ocorrências reais de
> `font: 600` no arquivo atual, nas linhas **:128, :595, :623, :710, :737** — listadas abaixo (linhas 2 a 6 da
> tabela) com seletor e papel do texto, no lugar dos números stale do brief.

| # | Seletor + arquivo:linha | Item da régua | O que está | O que deveria | Gravidade |
| --- | --- | --- | --- | --- | --- |
| 1 | `.pa-hero__rotulos b` / `.pa-hero__rotulos span` — `scw-participar-apoiar.css:122-128` | 2 | No herói de Participar (fundo magenta puro `#F10767`, `--scw-heroi-tinta` creme), a faixa "O que sua marca encontra / histórico de 16 edições" (coluna direita, abaixo do cartão de foto, ≥1000px) fica direto sobre o magenta, sem virar pílula chapada como o resto do herói. `b` = creme 100% a 11px/800 (~3,8:1, o mesmo par documentado no CLAUDE.md — insuficiente porque 11px não é "texto grande"). `span` = creme a 72% via `color-mix(...,transparent)` a 12px/600 — contraste ainda mais baixo que 3,8:1. | Aplicar o mesmo tratamento já usado em `.pa-hero__lead`, `.pa-hero__nota` e no botão `--contorno-claro` desta rota: fundo chapado (pílula) ou promover a texto grande — nunca tinta direta sobre o magenta. | alta |
| 2 | `.pa-hero__rotulos span` — `scw-participar-apoiar.css:128` | 4 | `font: 600 12px/1.2` — peso 600 no rótulo secundário "histórico de 16 edições" (coluna do herói). Mesma linha do achado #1 acima. | Peso 500, 700, 800 ou 900 — nunca 600. | média |
| 3 | `.pa-onde ul li` — `scw-participar-apoiar.css:595` | 4 | `font: 600 14.5px/1.35` — peso 600 nos itens de lista de "Apoiar 04 — onde a marca aparece" (bullets de locais de exposição da marca). Markup é da página Apoiar, não Participar, mas a regra vive no CSS compartilhado auditado aqui. | Peso 500/700/800/900. | média |
| 4 | `.pa-quem__lista li` — `scw-participar-apoiar.css:623` | 4 | `font: 600 clamp(15px,1.3vw,17px)/1.4` — peso 600 nos itens de lista de "Apoiar 05 — quem vive o festival". Também markup exclusivo de Apoiar, CSS compartilhado. | Peso 500/700/800/900. | média |
| 5 | `.pa-erro` — `scw-participar-apoiar.css:710` | 4 | `font: 600 12.5px/1.35` — peso 600 na mensagem de erro de validação (`role="alert"`) de cada campo do formulário de pré-cadastro do Participar (helper `campo()`, Participar.jsx). | Peso 500/700/800/900. | média |
| 6 | `.pa-form__status` — `scw-participar-apoiar.css:737` | 4 | `font: 600 14px/1.5` — peso 600 na mensagem de status do envio (`role="status"`, texto "Não conseguimos registrar agora…") do formulário de pré-cadastro do Participar. | Peso 500/700/800/900. | média |
| 7 | `JORNADA` (array) — `Participar.jsx:158-163`, pintado em `.pa-jornada li`/`b` — `scw-participar-apoiar.css:521,526` | 5 | Os 4 passos da seção "Jornada" (07) repetem cor entre vizinhos: 01 "Pré-cadastro" e 04 "Próximos passos" usam os dois `var(--scw-amarelo)`; 02 "Análise" e 03 "Contato e aprovação" usam os dois `var(--scw-cyan)` — inclusive adjacentes (02≡03). A cor pinta a borda superior de 3px e o numeral de cada passo, lado a lado numa grade de 4. | 4 cores distintas, nenhuma repetida. O fundo da seção é chocolate (`.scw-secao--choco`): por contraste documentado no próprio código (`nav.jsx`), roxo (~1,45:1) e marrom não sustentam aí, mas amarelo, cyan, magenta (texto grande, ~3,83:1) e laranja sustentam — 4 cores viáveis para 4 passos, sem precisar repetir nenhuma. | alta |
| 8 | `<ScwIcon nome="topicos/mensagem">` — `Participar.jsx:594` | 9 | O rótulo da seção "Pré-cadastro" (08, dentro de `.pa-form__intro`) usa `topicos/mensagem` — um avião de papel, ícone de "enviar mensagem" (rótulo do próprio registro: "mensagem"). O registro `scw-icons.js` já tem `mecanica/inscricao` — documento com linhas de lista, rotulado literalmente "inscrição" — semanticamente mais próximo do conteúdo real da seção (formulário de pré-cadastro), que não é sobre enviar uma mensagem. | Considerar `mecanica/inscricao` (ou outro ícone de formulário/documento da família `mecanica`/`topicos`) no lugar de `topicos/mensagem`. | média |
| 9 | `.pa-cabeca__link`, `.pa-destaque`, `.pa-sucesso button`, `.pa-barra` — `scw-participar-apoiar.css:198, 207, 783, 804` | 10 | Quatro transições/animações com duração escrita à mão em vez dos tokens `--mo-*`: 240ms nas linhas 198 e 783; 760ms + atraso de 240ms na linha 207 (`.pa-destaque`, usada em ~7 títulos da página inteira via `<em className="pa-destaque">`); 320ms com `cubic-bezier(.2,.7,.2,1)` próprio na linha 804 (`.pa-barra`, em vez de `var(--mo-ease)`/`var(--mo-mola)`). Nenhum valor bate com a escala 180/300/620/880ms. | Usar `var(--mo-rapido)` (180ms) ou `var(--mo-estado)` (300ms) para hover/estado, `var(--mo-entra)` (620ms) para entrada de texto, e `var(--mo-ease)`/`var(--mo-mola)` em vez da curva própria — conforme o princípio do próprio arquivo ("nenhuma duração é escrita à mão nos componentes", cabeçalho de `scw-motion.css`). | baixa |
| 10 | `DepoVideo` (componente) — `Participar.jsx:209-237` (definição), `:417-425` (uso) | 11 | Os 5 depoimentos em vídeo não têm `<track kind="captions">`; o único texto associado ao `<video>` é o `aria-label` (nome + marca + "no Sweet & Coffee Week" — não o conteúdo falado). Mitigante real: todo card com vídeo também mostra a citação completa (`d.frase`) em `<blockquote>` logo abaixo, funcionando como transcrição textual — mas sem `aria-describedby` ligando os dois nem rótulo de "transcrição", e não deu para confirmar (sem acesso ao arquivo de vídeo) se o texto é literal ao que é dito. | Legenda sincronizada (`<track>`) é o padrão pleno; no mínimo, ligar o blockquote ao vídeo via `aria-describedby` e/ou rotular como transcrição. | média |
| 11 | Página inteira / `.pa-hero` — media queries em `scw-participar-apoiar.css:155, 312, 421, 817` | 7 | Só os breakpoints 1000px (herói empilha em banda) e 900px (alvo de toque de 44px, `.pa-barra` some) aparecem no arquivo — confirmados corretos por leitura de código. 820px e 390px da escala canônica não são usados neste CSS; as grades (`pa-numeros`, `pa-cards`, `pa-depos`, `pa-jornada`, `pa-form__grade`) são fluidas via `auto-fit`/`minmax(min(100%,...))`, sem indício estático de precisar de breakpoint fixo extra. | Não identifiquei problema concreto no código — requer runtime (não rodei `tests/responsive.mjs` nem abri navegador, conforme instrução) para confirmar ausência de overflow/corte em 390px e nos pontos intermediários. | baixa |

## Resumo por gravidade (até 100 palavras)

**Alta (2):** no herói de Participar, a faixa "histórico de 16 edições" fica em texto pequeno direto sobre o magenta puro, sem a pílula chapada que o resto do herói usa (falha de contraste real, ≥1000px). Os 4 passos da seção Jornada repetem cor entre vizinhos (amarelo×2, cyan×2) quando 4 cores distintas caberiam.

**Média (7):** 5 usos de `font: 600` na Nexa Slab (peso banido) espalhados pelo CSS compartilhado — 3 deles em elementos do próprio Participar (rótulo do herói, erro de campo, status do formulário). Ícone do Pré-cadastro (avião de papel) não combina com "inscrição"; vídeos de depoimento sem legenda, mitigado por citação em texto ao lado.

**Baixa (2):** durações de transição escritas à mão em 4 lugares (fora da escala `--mo-*`); breakpoints 820/390 ausentes no arquivo, sem indício de problema real — requer runtime para fechar.
