# Achados — Home

7 achados: 0 alta, 4 média, 3 baixa.

| # | Seletor + arquivo:linha | Item da régua | O que está | O que deveria | Gravidade |
| --- | --- | --- | --- | --- | --- |
| 1 | `--base`/`--dest` inline — `Home.jsx:321,400,478,506,565` | 1 (paleta) | Cinco `style={{ '--base': '#3D1308', '--dest': '#F10767' }}` (e variações com `#FEF0DD`/`#FDBB1A`/`#01AFCC`) escrevem hex literal direto no JSX. Os valores estão todos na paleta aprovada — por isso passam no teste automatizado de hex — mas não seguem "sempre via `var(--scw-*)`". | Referenciar os tokens (`'--base': 'var(--scw-choco)'`, `'--dest': 'var(--scw-magenta)'` etc.); custom property em inline style aceita `var()` normalmente. | média |
| 2 | `.hm-num__nota` — `scw-home.css:388` | 2 (contraste) | `color: rgba(61, 19, 8, .6)` sobre o fundo bege da seção 05 Números (`scw-secao--bege`), texto 13,5px/peso 500 sem escala responsiva. Contraste calculado (composição alpha + luminância relativa) ≈ 4,10:1 contra `#F8E4C1`. | Texto pequeno precisa 4,5:1 AA. Subir a opacidade (≈.68–.7 chega a ~4,7–5:1) ou trocar por `var(--scw-marrom)` sólido, como as demais notas do sistema. | média |
| 3 | `.hm-rota__texto p`, `.hm-etapa__texto` — `scw-home.css:301,359` | 4 (tipografia) | Parágrafos de corpo (≤32ch) sem `text-wrap: pretty`, ao contrário de `.hm-apoio` e `.hm-galeria__texto`, que aplicam a regra na mesma página. | Regra do sistema é "parágrafo até 46ch com pretty" — aplicar `text-wrap: pretty` para consistência com as outras classes de corpo da Home. | baixa |
| 4 | `@media (max-width: 560px)` — `scw-home.css:706,721,727` | 7 (mobile) | O comentário da seção já rotula a escala própria da página como "1000 · 760 · 560 · 420" (linha 706) — 560px não está na escala canônica (1000·900·820·760·420) e não há comentário justificando um ponto próprio de conteúdo. | Escala canônica só admite valor fora dela quando o conteúdo exige um ponto específico (com justificativa registrada); caso contrário, mover a regra para 760 ou 420. | baixa |
| 5 | `.hm-etapa__texto` — `scw-home.css:359` | 7 (mobile / corpo) | `font: 500 14.5px/1.5` fixo, sem clamp nem breakpoint — abaixo do piso de 15px, e menor que as legendas irmãs da mesma página (`.hm-galeria__texto` e `.hm-rota__texto p`, ambas 15px). | "Corpo nunca <15px" — subir para 15px, alinhando com as demais legendas de card da Home. | média |
| 6 | Contador do `useEffect` — `Home.jsx:209` | 10 (movimento) | `(ts - inicio) / 1400` — duração de 1400ms cravada na animação de contagem dos números, sem relação com nenhum token (`--mo-rapido/estado/entra/longo` = 180/300/620/880ms). | Se for tratado como "entrada de bloco", usar `--mo-entra` (620ms) ou similar; se a contagem precisa de duração própria, registrar a exceção em comentário. | baixa |
| 7 | `IntersectionObserver` do destaque — `Home.jsx:239` | 10 (movimento) | `e.target.style.animation = 'scwDestaque 1150ms cubic-bezier(.33,.02,.2,1) 1100ms both'` — duração (1150ms), atraso (1100ms) e curva (`cubic-bezier(.33,.02,.2,1)`) todos hardcoded; nenhum bate com os tokens `--mo-*` nem com as curvas `--mo-ease`/`--mo-mola`/`--mo-suave`. | Consumir os tokens via string (ex. `` `scwDestaque var(--mo-longo) var(--mo-mola) var(--mo-atraso) both` ``), como o resto do motion system faz. | média |
